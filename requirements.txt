# Core web framework
flask==3.0.3
gunicorn==22.0.0

# Camera capture (headless — no display required)
opencv-python-headless==4.9.0.80
numpy==1.26.4

# Image generation for fallback frames
Pillow==10.4.0

# SSL certificate generation
cryptography==42.0.8

# System information
psutil==5.9.8
